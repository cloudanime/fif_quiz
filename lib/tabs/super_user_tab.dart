import 'package:flutter/material.dart';
import '../offline/super_user_dashboard.dart';

class SuperUserTab extends StatelessWidget {
  const SuperUserTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const SuperUserDashboard();
  }
}
